using Unity.VisualScripting;
using UnityEngine;

public class UI_GameScene : UI_Scene
{
    public override bool Init()
    {
        if (base.Init() == false)
            return false;
        
        
        
        return true;
    }

}